doctype html
html
  head
    title SmartReceipt Identity
    meta name='viewport' content='width=device-width, initial-scale=1.0'
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  body class=="#{controller.controller_name} #{controller.action_name}"
    #page.container-fluid
      .row-fluid
        .span12
          h1.brand
            a href==root_url
              span.sr_red Smart
              | Receipt
          #content
            = yield
    footer.brand
      small.pull-right
        ' Copyright
        a role='button' data-toggle='modal' href==(signed_in? ? '#toolbox' : sign_in_url)
          | &copy;
        |  SmartReceipt
    #shadow
    = render 'toolbox' if signed_in?
